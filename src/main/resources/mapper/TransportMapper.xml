<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.security.info.manage.mapper.TransportMapper">

    <select id="listTransport" resultType="com.security.info.manage.dto.res.TransportResDTO">
        select id, name, status, create_date
        from t_transport
        where is_delete=0
        order by create_date asc
    </select>

    <select id="listAllTransport" resultType="com.security.info.manage.dto.res.TransportResDTO">
        select id, name, status, create_date
        from t_transport
        where is_delete=0
        order by create_date asc
    </select>

    <select id="selectTransportIsExist" resultType="java.lang.Integer">
        SELECT count(1) FROM t_transport WHERE name=#{name} and is_delete=0
        <if test="id!=null">
            and id!=#{id}
        </if>
        limit 1
    </select>

    <update id="modifyTransport">
        update t_transport set
        <if test="name!=null">
            name=#{name},
        </if>
        <if test="status!=null">
            status=#{status},
        </if>
        update_by=#{userId}, update_date=now()
        where
        id=#{id} and is_delete=0
    </update>

    <insert id="addTransport">
        INSERT INTO t_transport (id, name, create_by, status)
        select #{id}, #{name}, #{userId}, #{status}
        FROM DUAL
        WHERE not exists (SELECT id FROM t_transport WHERE name=#{name} and is_delete=0)
    </insert>

    <update id="deleteTransport">
        update t_transport set
        is_delete=1, update_by=#{userId}, update_date=now()
        where id=#{id} and is_delete=0
    </update>

    <insert id="importTransport">
        <if test="list.size()>0">
            <foreach collection="list" index="index" item="riskAppliance" separator=";">
                INSERT INTO t_transport (id, name, create_by, status)
                select #{riskAppliance.id}, #{riskAppliance.name}, #{riskAppliance.userId}, #{riskAppliance.status}
                FROM DUAL
                WHERE not exists (SELECT id FROM t_transport WHERE name=#{riskAppliance.name} and is_delete=0)
            </foreach>
        </if>
    </insert>
</mapper>